type Ticket {
  id: ID!
  title: String!
  status: String!
}

input CreateTicketInput {
  title: String!
  status: String!
}

type Query {
  tickets: [Ticket!]!
  ticket(id: ID!): Ticket
}

type Mutation {
  createTicket(input: CreateTicketInput!): Ticket!
}
